__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
htmlcov/
.coverage
dist/
# The customer response page's build output. Named explicitly because `dist/` above matches only
# directories called exactly `dist`, so the second Vite entry's artifact would otherwise be committed.
dist-customer/
build/
node_modules/
docs/provider-findings.local.md
*.local.txt
local_credentials.txt
credentials.txt
credentials.local.txt
*.log

# tool caches and local measurement scratch (added when the tree was flattened to the repo root)
.import_linter_cache/
_demo_probe.py

